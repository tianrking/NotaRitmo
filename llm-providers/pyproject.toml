[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "notaritmo-llm-providers"
version = "0.1.0"
description = "Provider-neutral LLM contracts and safe reference adapters for NotaRitmo"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["llm_providers"]
